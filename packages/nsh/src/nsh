nsh() {
	file="$XDG_DATA_HOME/shells/$1.nix"
	if [[ ! -f "$file" ]]; then
		file="@share@/shells/$1.nix"
		if [[ ! -f "$file" ]]; then
			echo "No shell named '$1' defined."
			return 1
		fi
	fi

	nix-shell "$file"
}
